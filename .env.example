# Copied to /opt/dgx-llm-stack/.env by Ansible if the remote file does not exist.
# LITELLM_MASTER_KEY is intentionally omitted while stack.yml sets
# services.litellm.auth_enabled: false.

# The apply playbook copies HF_TOKEN from /home/ymin/spark-vllm-docker/HF-tk.env
# when this value is blank and that legacy file exists.
HF_TOKEN=

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-before-use
SEARXNG_SECRET_KEY=change-me-before-use
